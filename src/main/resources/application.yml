spring:
  datasource:
    url : "jdbc:mysql://localhost/board1"
    username : "root"
    password : "0070"
    driver-class-name : com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show_sql: true
      format_sql: true
      dialect: org.hibernate.dialect.MySQL8Dialect

